import DashboardLayout from "@/components/DashboardLayout";
import { SiteHeader } from "@/components/SiteHeader";
import { Button } from "@/components/ui/button";
import { useAuth } from "@/_core/hooks/useAuth";
import { startLogin } from "@/const";
import { trpc } from "@/lib/trpc";
import { BarChart3, CheckCircle2, Clock3, FileCheck2, LockKeyhole, Play, ShieldCheck } from "lucide-react";
import { toast } from "sonner";

function Metric({ icon: Icon, label, value, suffix = "%" }: { icon: typeof BarChart3; label: string; value: number; suffix?: string }) {
  return <article className="rounded-2xl border border-border bg-card p-5"><Icon className="size-5 text-primary" /><p className="mt-5 text-xs font-semibold tracking-[0.12em] text-muted-foreground">{label}</p><p className="mt-2 font-mono text-3xl font-bold tracking-[-0.05em] text-foreground">{value}{suffix}</p></article>;
}

function EvaluationContent() {
  const utils = trpc.useUtils();
  const overview = trpc.admin.evaluationOverview.useQuery();
  const retrievalQuality = trpc.admin.retrievalQualityOverview.useQuery();
  const run = trpc.admin.runFixedEvaluation.useMutation({
    onSuccess: result => { toast.success("质量检查已完成", { description: `已运行 ${result.metrics.caseCount} 个固定检查案例。` }); void utils.admin.evaluationOverview.invalidate(); },
    onError: error => toast.error("质量检查运行失败", { description: error.message }),
  });
  const metrics = overview.data?.metrics ?? { caseCount: 0, intentAccuracy: 0, citationCompleteness: 0, refusalCorrectness: 0, averageLatencyMs: 0 };
  const retrievalMetrics = retrievalQuality.data ?? { k: 3, caseCount: 0, recallAtK: 0, meanReciprocalRank: 0, averageLatencyMs: 0, observations: [] };
  return <div className="mx-auto max-w-6xl space-y-7 pb-10"><section className="overflow-hidden rounded-[1.75rem] bg-primary p-7 text-primary-foreground shadow-[0_22px_54px_-32px_rgba(45,33,60,0.85)] sm:p-9"><p className="text-xs font-semibold tracking-[0.15em] text-[#d8c8e7]">SERVICE QUALITY CONTROL</p><h1 className="mt-3 font-display text-4xl font-bold tracking-[-0.055em]">质量监控，基于实际运行记录。</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-[#eadff1]">管理员可以运行固定检查案例，持续核验客服的意图路由、规则来源、拒答边界和处理耗时。每次运行都会写入实际记录后再汇总指标。</p><Button className="mt-6 rounded-xl bg-white text-primary hover:bg-white/90" onClick={() => run.mutate()} disabled={run.isPending}><Play className="mr-2 size-4 fill-current" />{run.isPending ? "正在检查…" : "运行质量检查"}</Button></section><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-4"><Metric icon={CheckCircle2} label="意图路由一致率" value={metrics.intentAccuracy} /><Metric icon={FileCheck2} label="来源返回完整率" value={metrics.citationCompleteness} /><Metric icon={ShieldCheck} label="拒答边界符合率" value={metrics.refusalCorrectness} /><Metric icon={Clock3} label="平均处理时间" value={metrics.averageLatencyMs} suffix=" ms" /></section><section className="rounded-[1.5rem] border border-border bg-card p-5 sm:p-7"><div className="flex items-center gap-3"><BarChart3 className="size-5 text-primary" /><div><p className="text-xs font-semibold tracking-[0.14em] text-primary">RETRIEVAL QUALITY</p><h2 className="mt-1 font-display text-2xl font-bold tracking-[-0.04em]">规则检索质量监控</h2></div></div><p className="mt-4 text-sm leading-6 text-muted-foreground">该组固定规则问题会计算当前词项回退检索的 Recall@{retrievalMetrics.k} 和 MRR，用于发现规则库更新后的明显回归。它不替代真实用户研究，也不代表线上服务承诺。</p><div className="mt-5 grid gap-3 sm:grid-cols-3"><Metric icon={FileCheck2} label={`Recall@${retrievalMetrics.k}`} value={retrievalMetrics.recallAtK} /><Metric icon={CheckCircle2} label="MRR" value={retrievalMetrics.meanReciprocalRank} /><Metric icon={Clock3} label="平均检索时间" value={retrievalMetrics.averageLatencyMs} suffix=" ms" /></div><div className="mt-6 overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-y border-border bg-secondary/35 text-xs font-semibold text-muted-foreground"><tr><th className="px-4 py-3">固定检查问题</th><th className="px-4 py-3">对应规则</th><th className="px-4 py-3">首个相关排名</th><th className="px-4 py-3">Top-{retrievalMetrics.k} 返回</th></tr></thead><tbody>{retrievalMetrics.observations.map(item => <tr key={item.id} className="border-b border-border/70 last:border-b-0"><td className="px-4 py-4 text-foreground">{item.question}</td><td className="px-4 py-4 text-muted-foreground">{item.expectedDocumentTitle}</td><td className="px-4 py-4 font-mono text-primary">{item.firstRelevantRank ?? "未召回"}</td><td className="px-4 py-4 text-xs leading-5 text-muted-foreground">{item.retrievedDocumentTitles.join(" · ") || "无结果"}</td></tr>)}</tbody></table></div></section><section className="rounded-[1.5rem] border border-border bg-card p-5 sm:p-7"><div className="flex items-center gap-3"><BarChart3 className="size-5 text-primary" /><div><p className="text-xs font-semibold tracking-[0.14em] text-primary">CHECK CASES</p><h2 className="mt-1 font-display text-2xl font-bold tracking-[-0.04em]">固定检查案例与规则</h2></div></div><p className="mt-4 text-sm leading-6 text-muted-foreground">系统比对预期与实际意图，检查需要来源的规则问题是否返回对应文档，并确认无匹配、未登录订单与跨账户订单场景不会绕过安全边界。处理时间为本次服务端实际计时，不等同于服务等级承诺。</p><div className="mt-6 overflow-x-auto"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-y border-border bg-secondary/35 text-xs font-semibold text-muted-foreground"><tr><th className="px-4 py-3">类型</th><th className="px-4 py-3">固定问题</th><th className="px-4 py-3">预期路由</th><th className="px-4 py-3">预期处理</th></tr></thead><tbody>{(overview.data?.cases ?? []).map(item => <tr key={item.id} className="border-b border-border/70 last:border-b-0"><td className="px-4 py-4 font-mono text-xs text-primary">{item.caseType}</td><td className="px-4 py-4 text-foreground">{item.question}</td><td className="px-4 py-4 text-muted-foreground">{item.expectedIntent}</td><td className="px-4 py-4 text-muted-foreground">{item.expectedOutcome}</td></tr>)}</tbody></table></div></section></div>;
}

export default function Evaluation() {
  const { user, loading } = useAuth();
  if (loading) return <div className="min-h-screen bg-background" />;
  if (!user) return <div className="min-h-screen bg-background"><SiteHeader /><main className="container py-16"><div className="max-w-xl rounded-[1.5rem] border border-border bg-card p-8"><LockKeyhole className="size-6 text-primary" /><h1 className="mt-5 font-display text-3xl font-bold">管理员登录验证</h1><p className="mt-2 text-sm text-muted-foreground">质量监控仅在身份验证后可继续访问。</p><Button className="mt-6 rounded-xl" onClick={() => startLogin()}>前往登录</Button></div></main></div>;
  if (user.role !== "admin") return <div className="min-h-screen bg-background"><SiteHeader /><main className="container py-16"><div className="max-w-xl rounded-[1.5rem] border border-destructive/30 bg-card p-8"><LockKeyhole className="size-6 text-destructive" /><h1 className="mt-5 font-display text-3xl font-bold">此处仅限管理员</h1><p className="mt-2 text-sm text-muted-foreground">普通用户没有质量监控权限，服务端也会拒绝访问相关数据。</p></div></main></div>;
  return <DashboardLayout><EvaluationContent /></DashboardLayout>;
}
