# CampusMate GitHub 发布与录屏指南

## GitHub 发布前检查

发布前应确认仓库只包含源代码、数据库迁移、公开来源说明和明确标注的演示数据。请不要提交 `.env`、访问令牌、真实简历、个人订单、真实校方内部文件、未经授权的图片或录屏原片。请保留 `README.md` 中对参考商城和 UI/UX Skill 的 MIT 许可归属说明。

| 检查项 | 应达到的状态 |
|---|---|
| `pnpm test:all` | 全部通过（当前为 59 项 TypeScript + 7 项 Python Agent 测试）。 |
| `pnpm check` | TypeScript 无错误。 |
| 演示披露 | 首页、商品、订单和客服中明确标注模拟/演示边界。 |
| 来源声明 | README、项目说明页和知识库文档可追溯到公开 URL。 |
| 安全检查 | 无密钥、无真实个人数据、无伪造评价、无真实支付承诺。 |
| 展示数据 | 录屏前运行一次固定评测，使面板显示实际运行结果。 |

## 建议的 GitHub 提交顺序

建议先创建私有仓库核对环境和内容，再决定是否公开。首个公开提交应包含功能代码、迁移、文档和测试；后续提交分别围绕“商城与订单”“RAG 与 Agent”“评测与文档”组织，便于面试时复盘开发过程。提交信息应说明动机，例如 `feat: add grounded customer service with citations`，不要只写 `update` 或 `fix`。

## 3–5 分钟录屏脚本

| 时间 | 操作 | 讲述重点 |
|---|---|---|
| 00:00–00:25 | 展示首页与商品分类。 | “项目从校园二手交易出发，但核心目标是展示可信 AI 客服与权限边界。” |
| 00:25–00:55 | 搜索“教材”，进入商品详情。 | “商品与价格均为演示数据；公开浏览与登录后下单分离。” |
| 00:55–01:20 | 登录并创建模拟订单。 | “订单只按服务端当前会话查询；前端不传用户 ID，且所有内容均为演示数据。” |
| 01:20–01:45 | 进入个人中心，指出新订单出现在“我的模拟订单轨迹”，再查看“已发布的闲置物品”。 | “个人中心由 `profile.mine` 的受保护路由聚合：订单按 `buyerUserId` 查询，发布物品按 `sellerUserId` 查询。普通账户没有发布记录时会看到隔离空态。” |
| 01:45–02:25 | 进入 AI 客服，问“出售仿制品是否合规？”。 | “FastAPI/LangGraph 使用 Chroma 中的 BGE 中文预训练语义向量召回规则证据；Node 再基于片段回答，引用区显示公开来源和工作流。” |
| 02:25–02:50 | 询问无关问题并创建模拟工单。 | “低分检索不会让模型猜测，系统会准备转人工分支；登录账户可保存问题、摘要和工作流，但不会联系真实客服。” |
| 02:50–03:20 | 管理后台展示知识文档、上传、替换、失效与批量重建入口。 | “对象存储/MySQL 是事实源；公开规则同步进 Chroma 是可恢复的派生索引。新版本先同步成功，旧版本才失效；失效会同步删除 Chroma 分块，所有动作均限制 admin 并有审计。” |
| 03:20–03:50 | 运行固定评测并展示检索对比文档。 | “业务评测来自本次 `evaluationRuns`；另有 5 条中文同义改写检索集，BGE 是 4/5、旧哈希基线 3/5，说明当前演示语料上的差异而非生产准确率。” |
| 03:50–04:20 | 打开项目说明页。 | “这里可对应代码讲清楚 RAG、审计、角色校验、个人中心隔离，以及受管 TiDB 不支持触发器时的诚实取舍。” |

## 录屏注意事项

录制时不要遮掩“演示数据”“非正式校方政策”等提示。若评测分数不是 100%，应保留真实结果，并说明原因和下一步优化方案；比起人为美化，这更能体现工程判断。录制前可使用无痕窗口检查普通用户是否看不到管理员入口，并在屏幕上避免出现个人邮箱、浏览器书签、密码管理器或任何令牌。

## 个人中心可复现演示流程

1. 以同一演示账户登录，依次打开“全部商品”与任一商品详情，创建一笔明确标注为演示数据的模拟订单。
2. 从详情页或导航进入“我的订单”，确认订单号、商品快照与创建时间已经出现；这是订单写入后的第一处读取验证。
3. 打开“个人中心”，确认该订单也出现在“我的模拟订单轨迹”。此处的服务端聚合不接收前端用户 ID，而是从 OAuth 会话取当前账户并以 `buyerUserId` 过滤。
4. 若使用演示管理员账户，继续说明“已发布的闲置物品”中的展示中、已被预留和已下架状态；若使用普通账户，则展示空态并说明这是 `sellerUserId` 的所有权隔离，而非缺少前端数据。
5. 最后用未登录窗口访问 `/profile`，页面只显示“登录后管理你的校园流转”与登录入口。路由测试同时覆盖未认证调用 `profile.mine` 返回 `UNAUTHORIZED`，从 UI 和服务端两个层面证明个人数据不会匿名暴露。

## 面试追问速答

> **问：为什么不把 RAG 直接交给模型？**
>
> **答：** 模型只在检索到证据后负责压缩与表达。证据阈值与引用返回由程序控制，因此可以解释“为什么回答”“引用了什么”和“为什么拒答”。

> **问：为什么普通用户看不到后台还不够？**
>
> **答：** 前端入口可以被绕过。管理员 API 使用服务端 `adminProcedure`，普通用户构造请求同样会收到 `FORBIDDEN`；测试也覆盖该边界。

> **问：为什么已经有 BGE 还保留 TF-IDF？**
>
> **答：** BGE 改善近义表达召回，但多运行时首启、模型下载或加载失败时不能让客服不可用，更不能扩大个人数据权限。Node 保留 TF-IDF 作为受控回退；在 5 条固定中文改写中，BGE 为 4/5、哈希为 3/5，但这个小集不能外推为生产准确率。
